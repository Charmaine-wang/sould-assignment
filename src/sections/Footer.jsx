import React from 'react';
import styled from 'styled-components';

const StyledFooter = styled.div`
	display: flex;
`;

export const Footer = () => {
	return (
		<StyledFooter>
			<h1>Footer</h1>
		</StyledFooter>
	);
};
